<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Dev HTTP to PC backend on LAN (debug builds only). -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
